<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             x:Class="Chatter.Settings" BackgroundColor="#EEEEEE" NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <StackLayout>
            <Grid HeightRequest="35" Margin="0" Padding="0,10,0,5">
                <BoxView BackgroundColor="#EEEEEE" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="5"/>
                <Button BackgroundColor="Transparent" ImageSource="backbuttonred.png" Grid.Column="0" Grid.Row="0" x:Name="backButton" Clicked="backButton_Clicked"/>
                <Label FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start" FontSize="Medium" BackgroundColor="Transparent" Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" Text="Settings"/>
            </Grid>
            <ScrollView Padding="0,0,0,10">
                <StackLayout Spacing="10" Margin="25,0,25,10">
                    <Label Text="Discovery Settings" FontSize="Large" FontAttributes="Bold"></Label>
                    <Grid HorizontalOptions="CenterAndExpand" BackgroundColor="Transparent" Padding="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="AUTO"></RowDefinition>
                            <RowDefinition Height="AUTO"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Text="Location" Grid.Column="0" Grid.Row="0" FontSize="Medium"></Label>
                        <Picker x:Name="locationPicker" Grid.Column="0" Grid.Row="1" Title="Select Location" SelectedIndexChanged="Picker_SelectedIndexChanged">
                            <Picker.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>My Current Location</x:String>
                                    <x:String>Add a new location</x:String>
                                </x:Array>
                            </Picker.ItemsSource>
                        </Picker>
                    </Grid>
                    <Grid HorizontalOptions="CenterAndExpand" BackgroundColor="Transparent" Padding="10,5,10,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="AUTO"></RowDefinition>
                            <RowDefinition Height="AUTO"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Text="Show me" Grid.Column="0" Grid.Row="0" FontSize="Medium"></Label>
                        <Picker x:Name="showmePicker" Grid.Column="0" Grid.Row="1" Title="" SelectedIndexChanged="showmePicker_SelectedIndexChanged">
                            <Picker.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>Men</x:String>
                                    <x:String>Women</x:String>
                                    <x:String>Everyone</x:String>
                                </x:Array>
                            </Picker.ItemsSource>
                        </Picker>
                    </Grid>
                    <StackLayout HorizontalOptions="CenterAndExpand" Padding="10" BackgroundColor="Transparent">
                        <Grid HorizontalOptions="CenterAndExpand">
                            <Label Grid.Column="0" Text="Maximum Distance" FontSize="Medium" Grid.ColumnSpan="2"></Label>
                            <Label Grid.Column="1" HorizontalTextAlignment="End" FontSize="Body" Text="{Binding Source={x:Reference Name=slider}, Path=Value, StringFormat='{0:N0} Miles'}"></Label>
                        </Grid>
                        <Slider x:Name="slider" Value="{Binding maximum_distance, Mode=TwoWay}" Maximum="100" MaximumTrackColor="#98000B" MinimumHeightRequest="10" MinimumTrackColor="#98000B" ThumbColor="#98000B" ThumbImageSource="heartthumb.png"></Slider>
                    </StackLayout>
                    <StackLayout HorizontalOptions="CenterAndExpand" Padding="10" BackgroundColor="Transparent">
                        <Grid HorizontalOptions="CenterAndExpand">
                            <Label Grid.Column="0" Text="Age range" FontSize="Medium"></Label>
                            <Label Grid.Column="1" HorizontalTextAlignment="End" FontSize="Body" Text="{Binding Source={x:Reference Name=ageslider}, Path=Value, StringFormat='18 - {0:N0}'}" ></Label>
                        </Grid>
                        <Slider x:Name="ageslider" Value="{Binding age_range, Mode=TwoWay}" Maximum="60" Minimum="18" MaximumTrackColor="#98000B" MinimumHeightRequest="10" MinimumTrackColor="#98000B" ThumbColor="#98000b" ThumbImageSource="heartthumb.png"></Slider>
                    </StackLayout>
                    <Button x:Name="saveButton" Text="Save and Apply" Margin="0,0,0,10" CornerRadius="30" BackgroundColor="#e3e1de" Clicked="saveButton_Clicked"></Button>
                    <Label Text="Contact Us" FontSize="Large" FontAttributes="Bold"></Label>
                    <Button Text="Help and Support" CornerRadius="30" BackgroundColor="#e3e1de"></Button>
                    <StackLayout Padding="0,30,0,0" >
                        <Button Text="DELETE MY ACCOUNT" TextColor="#98000b" BackgroundColor="#EEEEEE" BorderColor="Accent" FontAttributes="Bold" BorderWidth="2" CornerRadius="30" Clicked="logoutButton_Clicked"></Button>
                        <Button Text="LOGOUT" TextColor="#EEEEEE" BackgroundColor="#98000b" CornerRadius="30" x:Name="logoutButton" Clicked="logoutButton_Clicked"></Button>
                    </StackLayout>
                    <Grid BackgroundColor="Transparent" Padding="0,50,0,0" VerticalOptions="EndAndExpand" HorizontalOptions="CenterAndExpand">
                        <Image Grid.Row="0" Source="FINAL_AMERA.png" WidthRequest="200"></Image>
                        <Label Grid.Row="1" Text="Version 1" FontSize="Micro" VerticalOptions="Start" HorizontalOptions="Center"></Label>
                    </Grid>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>